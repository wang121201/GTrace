# Imported source and notices

The import baseline is commit `0e21251f126510744d1b319f043e7e6b2dae5e1f`.
`source-map.json` records all 171 logical paths, effective overlay origins,
original SHA-256 values and copied SHA-256 values. The only import edits are
19 absolute include-path substitutions. Source comments and embedded notices
are retained unchanged.

The imported `nlohmann/json.hpp` retains its Niels Lohmann copyright and
`SPDX-License-Identifier: MIT` notices. The original source snapshots used for
the HBFSIM and GTSim inputs do not contain a standalone root LICENSE, COPYING,
or NOTICE file. This import does not invent or apply an umbrella license to
those sources. Their exact source origins remain recorded in `source-map.json`.

This repository is a new development copy. It does not alter the original
snapshots, change their author attribution, or publish a new upstream release.
